import GeneratedCodeLib.*;

public class ../ejemplos/Ejem1/ejem1.prg {
    public static void main(String args[]) {
        IntSet a, b, c, d
        int a1, b1, c1, d1
;

        a = ;
        b = ;
        c = unionSet(a, b);
        d = intersectionSet(a, b);
        a1 = card(a);
        
System.out.println("El valor de la variable " + a1 + " es: " + (value of identifier));        b1 = card(b);
        
System.out.println("El valor de la variable " + b1 + " es: " + (value of identifier));        c1 = card(c);
        
System.out.println("El valor de la variable " + c1 + " es: " + (value of identifier));        d1 = card(d);
        
System.out.println("El valor de la variable " + d1 + " es: " + (value of identifier));
    }

}
